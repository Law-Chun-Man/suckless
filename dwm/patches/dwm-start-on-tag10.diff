--- a/dwm.c
+++ b/dwm.c
@@ -635,7 +635,7 @@
 	Monitor *m;
 
 	m = ecalloc(1, sizeof(Monitor));
-	m->tagset[0] = m->tagset[1] = 1;
+    m->tagset[0] = m->tagset[1] = 1 << 9;
 	m->mfact = mfact;
 	m->nmaster = nmaster;
 	m->showbar = showbar;
